---
description: Harness project governance
alwaysApply: true
---

开工前先读项目根目录 `AGENTS.md`、`project-tracker.md` 和 `SECURITY.md`。

必须遵守：

1. 未确认需求前不改代码。
2. 不直接 push main / master。
3. 不读取或修改 `.env`、credentials、secrets。
4. 不执行破坏性命令。
5. 收工前更新 `project-tracker.md`，并说明验证结果。
